// Public destinations only. Never put API keys or customer-specific links here.
// Leave blank until the account owner verifies each destination and checkout price.
export const commercialConfig = Object.freeze({
  formEndpoint: '',
  bookingUrl: '',
  pilotPaymentUrl: '',
  supportPaymentUrl: '',
});
